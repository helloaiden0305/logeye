# Android 应用开发简答题

---

## 1. 冷启动 vs 热启动

### 完整启动流程（冷启动）

| 阶段 | 流程 | 耗时原因 |
|---|---|---|
| 系统侧 | 点击 Icon → AMS 通知 Zygote fork → ActivityThread.main() → Application.attach → onCreate | Zygote fork、进程创建 |
| 应用侧 | Activity.onCreate → onStart → onResume → performTraversals() {measure → layout → draw} | SDK 初始化、布局 inflate、视图层级测量绘制 |
| 用户感知 | 首帧（vsync）提交到 GPU → 屏幕逐行刷新 | — |

### 对比

| | 冷启动 | 温启动 | 热启动 |
|---|---|---|---|
| 进程 | **不存在**，需 Zygote fork 创建 | **已存在**，进程未回收 | **已存在**，进程未回收 |
| 流程 | Application.onCreate → Activity.onCreate → onStart → onResume → measure/layout/draw | onCreate → onStart → onResume → measure/layout/draw | onRestart → onStart → onResume |
| 耗时 | 最长（几百 ms ~ 数秒） | 中等 | 最短（几十 ms） |

**补充：**

- **冷启动实测起点**：Application.onCreate → Activity.onCreate → 首帧发出（vsync），优化核心在这条主线上
- **性能指标**：冷启动首帧耗时 = Application.onCreate + Activity.onCreate + measure/layout/draw，可通过 `adb shell am start -W` 测量 `Complete` 字段
- **常见坑**：Application.onCreate 中第三方 SDK（推送/统计/地图）同步初始化、布局嵌套过深、过度绘制、主线程 IO

---

## 2. 开机启动慢的原因

**启动流程概览**：按下电源 → Bootloader → Kernel → init → Zygote → SystemServer → Launcher

### 核心逻辑

一句话概括：**"内存低必慢，其他就是各阶段加载资源多少。"**

- 内存低 → preload / SystemServer 初始化时容易触发 Swap，频繁磁盘读写
- 存储 IOPS 瓶颈（eMMC/UFS）→ 大量文件扫描、资源加载受限
- Kernel 驱动加载慢 → 硬件初始化拖后腿

### 分阶段加载瓶颈

- **init 阶段**：
  - rc 脚本中 `service` 启动太多 native 层服务，串行执行阻塞后续启动
  - 大量 `exec` 动作同步执行（文件读写、磁盘检查）

- **Zygote 阶段**：
  - preload 预加载 Class / Resource / Framework 库耗时过长
  - 预加载文件过多（framework-res.apk、字体、WebView APK 等）

- **SystemServer 阶段**：
  - 系统服务逐个启动（PMS、AMS、WMS…），部分串行初始化
  - PMS 扫描 APK（system → system_ext → product → vendor → odm，后扫的覆盖先扫的，扫描量大则耗时）
  - 定制 ROM 中额外的厂商 Java 服务注册（由 SystemServer 显式调用，非 PMS 扫描）

- **Launcher 阶段**：
  - 桌面冷启动加载 Widget、壁纸、文件夹图标
  - 应用图标缓存重建

**定位方法**：

- `adb shell setprop persist.sys.loglevel 3` → 查看 `boottime` / `trace.txt`
- `systrace` 分析 init / Zygote / SystemServer 耗时
- 对比冷启动和 warm start 差值

---

## 3. 点击屏幕到响应的完整流程

1. **硬件层** — 触控 IC 捕获坐标，产生硬件中断
2. **驱动层** — 输入驱动转事件，写入 `/dev/input/eventX`
3. **InputManagerService (IMS)** — 系统服务读取事件，滤波、校准
4. **事件分发** — InputDispatcher → ViewRootImpl → 根 View（DecorView）
5. **事件传递链**：
   - `dispatchTouchEvent` → `onInterceptTouchEvent`（是否拦截）→ `onTouchEvent`（是否消费）
   - 自上而下：Window → DecorView → 父 View → 子 View
   - 自下而上：子 View → 父 View → Activity
6. **UI 更新** — `invalidate()` → draw（重绘）或 `requestLayout()` → measure → layout → draw

---

## 4. 如何把开源 APK 移植到定制 ROM 并赋予权限

**场景：** 手机厂商定制 ROM 或编译 LineageOS 等开源 ROM 时，将自定义应用打包进系统镜像并赋予系统级权限。

**操作步骤：**

1. **放入源码** — 将源码放入 `vendor/厂商名/apps/包名/` 或 `packages/apps/包名/`
2. **平台签名** — 编写 `Android.bp` 或 `Android.mk`，设置 `LOCAL_CERTIFICATE := platform`（平台签名）
3. **声明权限** — 在 `AndroidManifest.xml` 中 `<uses-permission>` 列出所需权限
4. **添加白名单** — `device/厂商/设备/privapp-permissions-设备.xml` 中写入包名和权限列表
5. **加入编译** — `device/厂商/设备/device.mk` 中 `PRODUCT_PACKAGES += 模块名`
